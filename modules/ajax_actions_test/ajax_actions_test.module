<?php

function ajax_actions_test_ajax_actions_commands(&$action, &$options) {
  if ($action['op'] == 'test') {
    $action['commands'][] = ajax_command_pp_popup('<span>' . $action['alert'] . '</span>');
  }
}